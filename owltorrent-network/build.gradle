plugins {
}

dependencies {
    implementation project(":owltorrent-common")

    compile group: 'org.apache.httpcomponents' , name: 'httpclient' , version: httpVersion
    compile group: 'com.dampcake', name: 'bencode', version: bencodeVersion
}
